# Environment: dev or prod
ENV=dev

# Database (dev defaults shown). Keep credentials here, not in code.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/mobius_rag

# GCS Bucket
GCS_BUCKET=mobius-rag-uploads-mobiusos

# --- LLM credentials: use env or a secret manager; do NOT store in PostgreSQL ---
# See docs/CREDENTIALS.md for where to store secrets.

# Ollama (local LLM). Must match a model you've pulled, e.g. ollama pull llama3.1:8b
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.1:8b
# OLLAMA_NUM_PREDICT=8192

# Vertex AI (Gemini) — recommended for production. Install: pip install -e ".[vertex]"
# VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_LOCATION=us-central1
# VERTEX_MODEL=gemini-1.5-pro
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# OpenAI (optional). Install: pip install -e ".[openai]"
# OPENAI_API_KEY=sk-...

# LLM_PROVIDER=ollama
# Critique retry: retry when score < threshold (0–1). UI can override via threshold input.
# CRITIQUE_RETRY_THRESHOLD=0.6
